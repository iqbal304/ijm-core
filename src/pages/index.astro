---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import EarthquakeProject from '../components/EarthquakeProject.astro';
import MusicPlayer from '../components/MusicPlayer.astro';
---

<Layout title="IJM Core - Web Portofolio Iqbal Jabar Marjan">
    <Navbar />
    
    <MusicPlayer />

    <main class="max-w-7xl mx-auto px-6 overflow-hidden">
        
        <Hero />

        <!-- Seksi Monitor Resonansi Aether -->
        <section id="observatory" class="py-20 reveal-scroll mt-20 relative z-10 border-t border-emerald-500/15">
            <div class="flex items-center gap-4 mb-12">
                <span class="text-cyan-400 font-mono text-lg font-bold">01.</span>
                <h2 class="text-2xl md:text-4xl font-bold text-white tracking-tighter drop-shadow-[0_0_10px_rgba(6,182,212,0.3)]">Monitor Resonansi Aether</h2>
                <div class="h-px bg-gradient-to-r from-emerald-500/30 to-transparent grow max-w-sm ml-4 hidden sm:block"></div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                
                <!-- Kartu Deskripsi Filosofis -->
                <div class="lg:col-span-1 flex flex-col gap-6">
                    <div class="bg-[#0a1118]/80 border border-emerald-500/20 p-6 xl:p-8 rounded-2xl backdrop-blur-md h-full flex flex-col justify-between shadow-[0_0_30px_rgba(0,0,0,0.5)]">
                        <div>
                            <div class="text-[9px] font-mono text-cyan-300 tracking-[0.2em] mb-4 flex items-center gap-2">
                                <span class="w-1.5 h-1.5 bg-cyan-400 rounded-full animate-ping"></span>
                                TELEMETRI BUMI AKTIF
                            </div>
                            <h3 class="text-xl md:text-2xl font-bold text-white mb-3">Integrasi API Seismik</h3>
                            <p class="text-xs md:text-sm text-zinc-300 font-light leading-relaxed mb-6">
                                Dari ketinggian Istana Langit, sistem Batu Levitas memantau getaran tektonik bumi secara *real-time*. Data mentah dari USGS diolah oleh *backend*, dikonversi menjadi koordinat visual, dan dipancarkan ke atas awan.
                            </p>
                        </div>
                        
                        <div class="font-mono text-[10px] md:text-xs text-zinc-400 bg-[#070b12] p-3.5 md:p-4 rounded-xl border border-emerald-500/20 overflow-x-auto shadow-inner">
                            <span class="text-cyan-400">fetch</span>('https://earthquake.../geojson')<br/>
                            &nbsp;&nbsp;.then(res => res.<span class="text-emerald-400">json</span>())<br/>
                            &nbsp;&nbsp;.then(data => renderAetherMap(data));
                        </div>
                    </div>
                </div>

                <!-- Map Komponen -->
                <div class="lg:col-span-2 relative">
                    <EarthquakeProject />
                </div>

            </div>
        </section>

    </main>

    <Footer />
</Layout>

<script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
        if (typeof window.gsap !== 'undefined') {
            window.gsap.registerPlugin(window.ScrollTrigger);
            
            const tl = window.gsap.timeline();
            tl.from(".header-nav", { y: -30, opacity: 0, duration: 1, ease: "power2.out" })
              .from(".hero-item", { y: 30, opacity: 0, duration: 1, stagger: 0.15, ease: "power3.out" }, "-=0.5"); 
        }
    });
</script>