---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Hero from '../components/Hero.astro';
import Footer from '../components/Footer.astro';
import EarthquakeProject from '../components/EarthquakeProject.astro';
import MusicPlayer from '../components/MusicPlayer.astro';
---

<Layout title="Laputa Core - Web Portofolio Iqbal Jabar">
    <Navbar />
    
    <MusicPlayer />

    <main class="max-w-7xl mx-auto px-6 overflow-hidden">
        
        <Hero />

        <!-- LAYOUT BARU: Meja Komando Langit (Top Description + Full-Width Map) -->
        <section id="observatory" class="py-24 reveal-scroll mt-16 relative z-10 border-t border-emerald-500/20">
            
            <!-- Bagian Atas: Header & Penjelasan Horizontal -->
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-start mb-12">
                <div class="lg:col-span-5 flex flex-col justify-center">
                    <div class="flex items-center gap-3 mb-3">
                        <span class="text-cyan-400 font-mono text-lg font-bold">01.</span>
                        <span class="text-[10px] font-mono text-emerald-400 tracking-[0.2em] uppercase bg-emerald-950/60 px-2.5 py-1 rounded border border-emerald-500/30">
                            TELEMETRI_BUMI
                        </span>
                    </div>
                    <h2 class="text-3xl sm:text-5xl font-bold text-white tracking-tighter drop-shadow-[0_0_15px_rgba(6,182,212,0.3)]">
                        Monitor Resonansi Aether
                    </h2>
                </div>

                <div class="lg:col-span-7 bg-[#0a1118]/60 backdrop-blur-md p-6 rounded-2xl border border-emerald-500/20 flex flex-col sm:flex-row gap-6 items-center justify-between shadow-lg">
                    <p class="text-xs sm:text-sm text-zinc-300 font-light leading-relaxed">
                        Dari ketinggian Istana Langit, sistem Batu Levitas memantau getaran tektonik bumi secara <span class="text-cyan-300 font-medium">real-time</span>. Data mentah USGS diolah dan dipancarkan ke atas awan.
                    </p>
                    <div class="font-mono text-[10px] text-zinc-400 bg-[#070b12] p-3 rounded-xl border border-emerald-500/20 shrink-0 w-full sm:w-auto text-center sm:text-left shadow-inner">
                        <span class="text-cyan-400">fetch</span>('.../geojson')<br/>
                        &nbsp;.then(res => res.<span class="text-emerald-400">json</span>())
                    </div>
                </div>
            </div>

            <!-- Bagian Bauth: Map Meja Komando Full-Width -->
            <div class="w-full h-[450px] lg:h-[550px] rounded-3xl overflow-hidden shadow-[0_0_40px_rgba(16,185,129,0.1)] border border-emerald-500/30 relative">
                <EarthquakeProject />
            </div>

        </section>

    </main>

    <Footer />
</Layout>

<script is:inline>
    document.addEventListener("DOMContentLoaded", () => {
        if (typeof window.gsap !== 'undefined') {
            window.gsap.registerPlugin(window.ScrollTrigger);
            
            const tl = window.gsap.timeline();
            tl.from(".header-nav", { y: -30, opacity: 0, duration: 1, ease: "power2.out" })
              .from(".hero-item", { y: 30, opacity: 0, duration: 1, stagger: 0.15, ease: "power3.out" }, "-=0.5"); 
        }
    });
</script>